# Local AI — مساعد ذكي محلي

نظام مساعد ذكي يعمل محلياً بالكامل على جهازك باستخدام Ollama و ChromaDB، مع دعم RAG (Retrieval-Augmented Generation) من الملفات النصية، وذاكرة طويلة المدى لحفظ المعلومات الشخصية.

## ✨ المميزات

- 💬 **محادثة ذكية** مع نماذج Llama عبر Ollama
- 📚 **RAG من الملفات** — رفع ملفات (TXT, PDF, DOCX, MD, XLSX, XLS) والبحث فيها
- 🧠 **ذاكرة طويلة المدى** — حفظ واسترجاع المعلومات الشخصية تلقائياً
- 🔒 **خصوصية كاملة** — كل البيانات محلية على جهازك
- 🌐 **واجهة ويب** — واجهة سهلة الاستخدام على http://localhost:5050

## 📋 المتطلبات

- Python 3.8 أو أحدث
- Ollama (مثبت ومشغل)
- حزمة Python المطلوبة (انظر التثبيت)

## 🚀 التثبيت

### 1. تثبيت Ollama

**Windows:**
1. حمّل Ollama من [ollama.com](https://ollama.com)
2. ثبّت البرنامج
3. افتح Terminal أو PowerShell واكتب:
   ```bash
   ollama serve
   ```
4. اترك التيرمينال مفتوح (الخدمة تعمل في الخلفية)

**macOS / Linux:**
```bash
curl -fsSL https://ollama.com/install.sh | sh
ollama serve
```

### 2. تنزيل الموديلات المطلوبة

في تيرمينال جديد (بينما ollama serve مفتوح):

```bash
ollama pull llama3.2:3b
ollama pull nomic-embed-text
```

- `llama3.2:3b` — موديل المحادثة
- `nomic-embed-text` — موديل التضمينات للـ RAG

### 3. تثبيت مكتبات Python

```bash
cd local-ai
pip install -r requirements.txt
```

أو يدوياً:

```bash
pip install flask werkzeug chromadb ollama PyPDF2 python-docx openpyxl xlrd
```

## 🎯 التشغيل

```bash
python app.py
```

بعد التشغيل، افتح المتصفح على:
```
http://localhost:5050
```

## 📖 طريقة الاستخدام

### رفع الملفات والفهرسة

1. في الصفحة الرئيسية، اضغط على **"اختر ملفات"**
2. اختر الملفات المراد رفعها (TXT, PDF, DOCX, MD, XLSX, XLS)
3. اضغط **"فهرسة الملفات"** لمعالجتها وإضافتها للبحث
4. انتظر حتى تكتمل الفهرسة

### المحادثة مع الذاكرة والـ RAG

- اكتب سؤالك في مربع المحادثة
- اختر الموديل (افتراضي: `llama3.2:3b`)
- فعّل **"استخدام RAG"** لو عايز البحث في الملفات
- فعّل **"حفظ تلقائي"** عشان يحفظ المعلومات من الرسائل

### إدارة الذاكرة

- اذهب لصفحة **"الذاكرة"**
- يمكنك:
  - عرض كل الذاكرات المحفوظة
  - إضافة ذاكرة جديدة (مفتاح + قيمة)
  - تعديل ذاكرة موجودة
  - حذف ذاكرة
  - مسح كل الذاكرات

### حالة النظام

- اذهب لصفحة **"الحالة"**
- تعرض:
  - حالة Ollama (مشغّل أو لا)
  - الموديلات المتاحة
  - عدد الـ chunks المفهرسة

## 📁 هيكل المشروع

```
local-ai/
├── app.py          # تطبيق Flask الرئيسي
├── rag.py          # منطق RAG والبحث
├── memory.py       # إدارة الذاكرة
├── files/          # الملفات المرفوعة
├── db/             # قاعدة بيانات ChromaDB
├── templates/      # قوالب HTML
│   └── index.html
└── README.md       # هذا الملف
```

## 🛠️ API

### `/chat` (POST)

طلب محادثة مع الـ AI.

```json
{
  "messages": [
    {"role": "user", "content": "سؤالك هنا"}
  ],
  "model": "llama3.2:3b",
  "use_rag": true,
  "auto_memory": false
}
```

### `/memories` (GET)

عرض كل الذاكرات.

### `/memories/add` (POST)

إضافة ذاكرة جديدة.

```json
{
  "key": "اسم المفتاح",
  "value": "القيمة"
}
```

### `/memories/update` (POST)

تعديل ذاكرة.

```json
{
  "id": 1,
  "key": "مفتاح جديد",
  "value": "قيمة جديدة"
}
```

### `/memories/delete` (POST)

حذف ذاكرة.

```json
{
  "id": 1
}
```

### `/upload` (POST)

رفع ملفات.

### `/index_docs` (POST)

فهرسة الملفات المرفوعة.

### `/status` (GET)

حالة النظام.

## ⚙️ التخصيص

### تغيير الموديلات

في `rag.py`:

```python
EMBED_MODEL = "nomic-embed-text"  # موديل التضمينات
# في طلب chat:
model = "llama3.2:3b"  # موديل المحادثة
```

### تغيير المنفذ (Port)

في `app.py`:

```python
app.run(host="0.0.0.0", port=5050, debug=True, use_reloader=False)
```

غيّر `5050` للمنفذ المطلوب.

## 🐛 استكشاف الأخطاء

### Ollama غير مشغّل

- تأكد أن `ollama serve` مفتوح في تيرمينال
- جرّب إعادة تشغيل Ollama
- تأكد من تنزيل الموديلات المطلوبة

### خطأ في الاتصال

```bash
# تأكد أن Ollama يعمل
ollama list
```

### مشاكل في الفهرسة

- تأكد من وجود الملفات في مجلد `files/`
- تأكد من تثبيت المكتبات المطلوبة لقراءة الملفات (PyPDF2, python-docx, openpyxl, xlrd)

## 📝 ملاحظات

- النظام يعمل محلياً بالكامل — لا يرسل أي بيانات للإنترنت
- الموديلات الكبيرة قد تحتاج ذاكرة وصول (RAM) أكبر
- يمكن استخدام موديلات أخرى من Ollama (مثل `llama3.2:1b` للأجهزة الضعيفة)

## 📄 الترخيص

مشروع مفتوح المصدر للاستخدام الشخصي.

## 🤝 المساهمة

المساهمات مرحب بها! افتح Issue أو Pull Request.

---

صُنع بـ ❤️ للمساعدات الذكية المحلية
